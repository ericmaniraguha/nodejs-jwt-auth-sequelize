# Start nodejs JWT authentication with Sequelize, using Postgres

1. Steps

### a. npm init

### b. npm install express sequelize pg pg-hstore cors jsonwebtoken bcryptjs --save

npm install dotenv --save
